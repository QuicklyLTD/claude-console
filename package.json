{
  "name": "claude-console",
  "version": "0.1.0",
  "private": true,
  "description": "Feature-complete web console for Claude Code — assistant-ui + @anthropic-ai/claude-agent-sdk over persistent WebSocket.",
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "clean": "rm -rf server/dist client/dist data/console.db*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18"
  }
}
